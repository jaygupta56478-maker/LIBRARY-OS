# Library OS frontend

## Development

```powershell
npm install
npm run dev
```

The Vite dev server proxies `/api` to the C++ service at `http://localhost:8080`.

For the complete college-project workflow, run the C++ backend first, then the frontend.
